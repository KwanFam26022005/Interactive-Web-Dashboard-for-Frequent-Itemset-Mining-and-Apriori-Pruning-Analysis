# Bản Đồ Bằng Chứng Thực Nghiệm (Claim-Evidence Traceability Map)

Tài liệu này thiết lập mối liên kết truy xuất nguồn gốc chặt chẽ giữa các tuyên bố học thuật trong Báo cáo Giữa kỳ với các tệp dữ liệu thực nghiệm, biểu đồ vector, bảng dữ liệu và mã cam kết (Git commit) đã được đóng băng chính thức.

---

## Bảng Tra Cứu Tuyên Bố Học Thuật (Claim-Evidence Matrix)

| Claim ID | RQ | Nội Dung Tuyên Bố Học Thuật (Claim Summary) | Bằng Chứng Dẫn Xuất (Figure / Table) | Tệp Nguồn Xử Lý & Thô (Source Artifacts) | Git Commit / Lineage | Diễn Đạt Được Phép (Allowed Academic Phrasing) | Diễn Đạt Bị Cấm (Forbidden Overclaim) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **RQ1-C1** | RQ1 | Khi giảm ngưỡng hỗ trợ từ 0.60 xuống 0.35, số lượng ứng viên sinh ra mở rộng đáng kể từ 185 lên 2,131 ứng viên. | Bảng T1, Hình F2 | `mushroom_support_summary.csv` (`1b60921a...`) | Phase 4C Commit `b40022fd...` | "Số lượng ứng viên sinh ra mở rộng đáng kể (substantial expansion)..." | "Số lượng ứng viên bùng nổ theo hàm mũ (exponential explosion)..." (chưa khớp mô hình toán) |
| **RQ1-C2** | RQ1 | Số lượng tập mục phổ biến tăng từ 51 lên 1,189 và số lượng luật kết hợp phát hiện tăng từ 223 lên 11,055 khi giảm ngưỡng hỗ trợ từ 0.60 xuống 0.35. | Bảng T1, Hình F3 | `mushroom_support_summary.csv` (`1b60921a...`) | Phase 4C Commit `b40022fd...` | "Khối lượng mẫu trích xuất gia tăng mạnh mẽ trên tập dữ liệu Mushroom..." | "Tập mục phổ biến tăng trưởng siêu tuyến tính (super-linear)..." |
| **RQ1-C3** | RQ1 | Thời gian thực thi trung vị của thuật toán Apriori tăng từ 523.072 ms lên 14,047.443 ms khi giảm ngưỡng hỗ trợ từ 0.60 xuống 0.35. | Bảng T1, Hình F1 | `mushroom_support_summary.csv` (`1b60921a...`), `mushroom_support_runs.csv` (`022a56cb...`) | Phase 4C Commit `b40022fd...` | "Thời gian thực thi trung vị tăng tương ứng với khối lượng đánh giá độ hỗ trợ trên tập giao dịch..." | "Thời gian tăng do độ trễ truy vấn cơ sở dữ liệu (database counting overhead)..." (thực nghiệm chạy in-memory) |
| **RQ2-C1** | RQ2 | Tỷ lệ cắt tỉa ứng viên tổng thể tăng từ 5.95% (tại support 0.60) lên 29.28% (tại support 0.35). | Bảng T2, Hình F2, Hình F4 | `mushroom_support_summary.csv` (`1b60921a...`) | Phase 4C Commit `b40022fd...` | "Tỷ lệ cắt tỉa ứng viên tăng từ 5.95% lên 29.28% trong phạm vi ngưỡng hỗ trợ được khảo sát..." | "Cơ chế cắt tỉa giúp thuật toán chạy nhanh hơn 29.28%..." (thiếu đường cơ sở không cắt tỉa để đo thời gian) |
| **RQ2-C2** | RQ2 | Giai đoạn kết nối - cắt tỉa (join-prune) áp dụng từ $k \ge 2$. Cắt tỉa tập con thực tế bắt đầu loại bỏ ứng viên từ $k = 3$; tại $k = 2$ không có ứng viên nào bị loại bỏ. | Bảng T2b, Hình F4 | `mushroom_pruning_summary.csv` (`b89a2fb9...`), `mushroom_pruning_levels.csv` (`613632ed...`) | Phase 4C Commit `b40022fd...` | "Trong các quan sát hình thức, việc cắt tỉa tập con không phổ biến bắt đầu loại bỏ ứng viên từ bậc $k = 3$; bậc $k = 2$ không sinh ứng viên bị tỉa..." | "Cơ chế cắt tỉa chỉ hoạt động từ bậc $k \ge 3$..." (về thuật toán join-prune áp dụng từ $k \ge 2$, nhưng $k=2$ có tỉ lệ tỉa bằng 0 do mọi đơn mục đã phổ biến) |
| **RQ2-C3** | RQ2 | Tại các bậc độ dài $k \ge 3$, cơ chế cắt tỉa loại bỏ lên tới 50.0% ứng viên ($k=5$, support 0.60) và tối đa 243 ứng viên ($k=4$, support 0.35). | Bảng T2b, Hình F4 | `mushroom_pruning_summary.csv` (`b89a2fb9...`) | Phase 4C Commit `b40022fd...` | "Cắt tỉa Apriori loại bỏ hàng trăm ứng viên không tiềm năng trước bước đếm giao dịch..." | "Cắt tỉa đảm bảo số lượng ứng viên luôn đơn điệu giảm theo $k$..." (tính đơn điệu là đặc tính chẩn đoán trên tập dữ liệu này, không phải định lý bất biến) |
| **RQ3-C1** | RQ3 | Ở khối lượng dữ liệu nhỏ ($N \le 1,000$), độ trễ trung vị đo được nằm trong khoảng lượng tử hóa khung hình ($18.200\text{ ms} - 33.350\text{ ms}$) và không thiết lập một thứ tự ổn định duy nhất giữa các thư viện. | Bảng T3, Hình F5, Hình F6 | `visualization_summary.csv` (`8628fb95...`), `visualization_runs.csv` (`9e80833a...`) | Run `dea90c09...`, Capture `cb34745b...`, Acceptance `614658dc...` | "Tại $N \le 1,000$, các quan sát trung vị duy trì gần mức lượng tử hóa khung hình và không thiết lập một trật tự ổn định duy nhất xuyên suốt các thư viện và thao tác." | "Ba thư viện có độ phản hồi thị giác hoàn toàn tương đương nhau (indistinguishable visual responsiveness)..." (không làm thực nghiệm đo nhận thức thị giác người dùng) |
| **RQ3-C2** | RQ3 | Ở khối lượng dữ liệu lớn ($N = 10,000$), Chart.js ghi nhận độ trễ trung vị 25.900 ms render / 33.400 ms update; D3.js ghi nhận 72.600 ms render / 64.550 ms update; Apache ECharts đạt 88.500 ms render / 94.050 ms update. | Bảng T3, Hình F5, Hình F6 | `visualization_summary.csv` (`8628fb95...`) | Run `dea90c09...`, Capture `cb34745b...`, Acceptance `614658dc...` | "Dưới môi trường Edge 151 và khối lượng điểm phân tán cố định tại $N = 10,000$, Chart.js đạt độ trễ trung vị thấp nhất, tiếp theo là D3 SVG và ECharts Canvas..." | "Chart.js luôn vượt trội hơn D3 và ECharts trong mọi ứng dụng web..." / "D3 chậm hơn thuần túy do quá tải DOM của SVG..." |
| **RQ3-C3** | RQ3 | Thước đo thời gian của bài toán trực quan hóa là độ trễ quan sát hai khung hình (render-to-two-frame-observation latency). | Bảng T3, Hình F5, Hình F6 | `visualization_benchmark_config.json`, `benchmark.js` | Run `dea90c09...`, Capture `cb34745b...`, Acceptance `614658dc...` | "Thước đo đo lường độ trễ từ lúc kích hoạt vẽ đến khi hoàn tất 2 khung hình `requestAnimationFrame` liên tiếp..." | "Thước đo đo lường thời gian hoàn tất phần cứng GPU (GPU completion) hoặc thời gian quét điểm ảnh (paint completion / presentation completion)..." |
| **RQ3-C4** | RQ3 | Mối tương quan giữa độ trễ khởi tạo (render) và cập nhật (update) phụ thuộc vào kiến trúc thư viện và quy mô dữ liệu; dữ liệu thực nghiệm chuẩn hoá không hỗ trợ tuyên bố chung rằng cập nhật luôn nhanh hơn khởi tạo ban đầu. | Bảng T3, Hình F5, Hình F6 | `visualization_summary.csv` (`8628fb95...`) | Run `dea90c09...`, Capture `cb34745b...`, Acceptance `614658dc...` | "Mối quan hệ giữa trung vị khởi tạo và cập nhật phụ thuộc vào thư viện và quy mô; dữ liệu chấp thuận không hỗ trợ nhận định chung rằng cập nhật tại chỗ luôn nhanh hơn khởi dựng ban đầu." | "Cập nhật dữ liệu luôn luôn nhanh hơn vẽ biểu đồ lần đầu trong mọi trường hợp..." / "Ở $N \ge 5,000$ độ trễ cập nhật luôn thấp hơn khởi tạo trên cả 3 thư viện..." |
